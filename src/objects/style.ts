import { StyleSheet } from "react-native";

export const TextInputCSS = StyleSheet.create({
    container: {
        height: 50,  
        borderWidth: 1,
        borderRadius: 5,
        borderColor: '#000'
    },
});
